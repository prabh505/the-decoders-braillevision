Put 3-10 REAL embossed-Braille photos here (from your capture rig).
Then run, from the repo root:
  python inference.py --source sample_inputs/ --weights model/best.pt
Annotated images + decoded .txt files will be written to ../sample_outputs/.
Commit both folders so judges can see real inputs and outputs.
